import { MaxIntDirective } from './max-int.directive';

describe('MaxIntDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxIntDirective();
    expect(directive).toBeTruthy();
  });
});
