import { MaxFloatDirective } from './max-float.directive';

describe('MaxFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxFloatDirective();
    expect(directive).toBeTruthy();
  });
});
