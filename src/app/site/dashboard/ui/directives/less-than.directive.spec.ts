import { LessThanDirective } from './less-than.directive';

describe('LessThanDirective', () => {
  it('should create an instance', () => {
    const directive = new LessThanDirective();
    expect(directive).toBeTruthy();
  });
});
